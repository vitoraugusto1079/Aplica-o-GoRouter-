name: streaming
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0 # Gerenciamento do banco e autenticação
  go_router: ^13.2.0       # Gerenciamento de rotas obrigatório

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# Deixamos apenas UM bloco flutter aqui no final com tudo dentro!
flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/img/